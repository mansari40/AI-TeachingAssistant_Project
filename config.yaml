data:
  pdf_dir: data/raw/books
  artifacts_dir: data/artifacts

indexing:
  chunk_chars: 2800
  chunk_overlap: 350
  max_chunks_per_doc: 400

retrieval:
  top_k: 8
  min_score: 0.15

models:
  embed_model: text-embedding-3-large
  chat_model: gpt-4.1-mini
  embedding_dim: 3072
